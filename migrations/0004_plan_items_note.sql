-- ============================================================
-- 0004_plan_items_note.sql —— 计划项动作级备注列
-- ============================================================
-- 【教学说明】
-- 需求：计划里每个动作可以有独立备注（如"深蹲行程深一些"、
--   "硬拉70kg晋级赛"），区别于 plans.note（整计划备注）。
--
-- ⚠️ SQLite ALTER TABLE ADD COLUMN 的注意点（同 0003）：
--   已有行的新列值 = NULL（无法加 NOT NULL 默认值约束的旧行）。
--   所以 Rust 侧 PlanItem 新字段必须 Option<String>。
--
-- 迁移机制回顾：sqlx 检查 _sqlx_migrations 表，0001~0003 已执行过 → 跳过；
--   只执行本文件 0004 → 老数据库自动升级，无需删库。
-- ============================================================

-- 动作备注（空 = 该动作无备注，record_form 不显示）
ALTER TABLE plan_items ADD COLUMN plan_note TEXT;
